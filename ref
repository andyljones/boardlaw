#!/opt/conda/bin/python
import git
from rebar import storing

commit = str(git.Repo().head.commit)[:6]
run = storing.runs().iloc[-1]['run_name']
print(f'[[Safety Scaling Laws#Ref]] (`{commit}`):')
print(f'[[Safety Scaling Laws#Runs]] (`{commit}`, `{run}`):')
